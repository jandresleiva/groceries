node { 
    stage('Build') { 
        sh 'dotnet build'
    }
    stage('Test') {
        sh 'cd GroceriesTesting'
        sh 'dotnet test'
    }
    if (currentBuild.currentResult == 'SUCCESS') {
        stage('Deploy') {
            echo "SUCCEDED!!!"
        }
    }
}